import { TipoAssuntoPipe } from './tipo-assunto.pipe';

describe('TipoAssuntoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoAssuntoPipe();
    expect(pipe).toBeTruthy();
  });
});
